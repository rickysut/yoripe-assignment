# API bahasoweb 

```
composer install
cp .env.example .env
php artisan key:generate
php artisan migrate
php artisan db:seed (optional)
npm install
npm run build
php artisan serve

note: 

folder info/postman-test are postman sample requests

```
